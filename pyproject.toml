[project]
name = "distal"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff.lint]
select = ["E", "F", "I"]
